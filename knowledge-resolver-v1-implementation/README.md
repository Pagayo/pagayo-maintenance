# Knowledge Resolver — delivery mirror

Local-only sources (not in git):

- `pagayo-vault/knowledge-os/runtime/` — resolver runtime (V1 + V2)
- `pagayo-vault/knowledge-os/03-registry/canon-registry.yaml`
- `pagayo-vault/knowledge-os/03-registry/topic-registry.yaml`

This folder is the committable delivery mirror for lane `feature/admin-v2-skills`.

V2 adds `resolveCapability()` with capability → topic mapping in `topic-registry.yaml`.
